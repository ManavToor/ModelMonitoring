import pandas as pd

def run_query() -> (pd.DataFrame, pd.DataFrame):
    """
    get data from SQL server or wherever data is stored
    data should contain training data and current data with model predictions
    """
    return pd.DataFrame(), pd.DataFrame